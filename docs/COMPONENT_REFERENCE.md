# Slate Component Reference

> Auto-generated reference for every component recognised by the
> [SCF v1.0 schema](../schemas/scf-v1.0.schema.json).
> Each component with a `$defs` entry has a **full prop table** below;
> components without one accept arbitrary props (schema enforcement pending).

---

## Quick-Nav

| Component | Category | Props Schema |
|-----------|----------|:------------:|
| [TitleCard](#titlecard) | Core | ✅ |
| [BrandIntro](#brandintro) | Core | ✅ |
| [BrandOutro](#brandoutro) | Core | ✅ |
| [LowerThird](#lowerthird) | Core | ✅ |
| [AnimatedCaption](#animatedcaption) | Core | ✅ |
| [MetricsCard](#metricscard) | Data | ✅ |
| [PricingTable](#pricingtable) | Data | ✅ |
| [CompetitiveMatrix](#competitivematrix) | Data | ✅ |
| [ROICalculator](#roicalculator) | Data | ✅ |
| [BurnDown](#burndown) | Data | ✅ |
| [OKRStatus](#okrstatus) | Data | ✅ |
| [ProgressBar](#progressbar) | Data | ✅ |
| [VSCodeScene](#vscodescene) | IDE / Demo | ✅ |
| [AzureDevOpsScene](#azuredevopsscene) | IDE / Demo | — |
| [TerminalScene](#terminalscene) | IDE / Demo | ✅ |
| [TerminalCast](#terminalcast) | IDE / Demo | ✅ |
| [KustoExplorerScene](#kustoexplorerscene) | IDE / Demo | — |
| [ScreenDemoFrame](#screendemoframe) | IDE / Demo | ✅ |
| [CTABlock](#ctablock) | Engagement | ✅ |
| [CustomerStory](#customerstory) | Engagement | ✅ |
| [Quiz](#quiz) | Engagement | ✅ |
| [AskTheAudience](#asktheaudience) | Engagement | ✅ |
| MetricStack | Composite | — |
| BookPageMetrics | Composite | — |
| ComponentOverlay | Composite | — |
| ThreeScene | 3D / Premium Motion | — |
| DeviceStage3D | 3D / Premium Motion | — |
| HTMLTextureWall | 3D / Premium Motion | — |
| [Roadmap](#roadmap) | Planning | ✅ |
| [ReleaseNotes](#releasenotes) | Planning | ✅ |
| [TerminologyCard](#terminologycard) | Education | ✅ |
| [PresenterBug](#presenterbug) | Overlay | ✅ |
| [EventBranding](#eventbranding) | Overlay | ✅ |

Components without prop schemas are listed in [§ Unschematised Components](#unschematised-components) at the end.

---

## Core Components

### TitleCard

Full-screen title slide with optional subtitle and custom background.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `title` | string | ✅ | — | Main heading text |
| `subtitle` | string | | — | Secondary text below the title |
| `background` | string | | — | CSS colour or image URL |

```jsonc
{
  "component": "TitleCard",
  "props": { "title": "Welcome to Slate", "subtitle": "Enterprise Video Production" }
}
```

---

### BrandIntro

Animated logo reveal with company name and tagline.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `logoSrc` | string | ✅ | — | Path or URL to logo image |
| `companyName` | string | ✅ | — | Company name text |
| `tagline` | string | | — | Tagline shown below the company name |
| `primaryColor` | string | | — | CSS colour for brand accent |

```jsonc
{
  "component": "BrandIntro",
  "props": { "logoSrc": "assets/logo.png", "companyName": "Contoso", "tagline": "Innovation Everywhere" }
}
```

---

### BrandOutro

Closing card with CTA, contact info, and branding.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `companyName` | string | ✅ | — | Company name text |
| `logoSrc` | string | | — | Path or URL to logo |
| `primaryColor` | string | | — | CSS colour for brand accent |
| `cta` | string | | — | Call-to-action text |
| `ctaText` | string | | — | Alternate CTA text (used in some templates) |
| `contact` | string | | — | Contact info string |
| `tagline` | string | | — | Tagline text |

> **Gotcha:** Some SCF files use `ctaText` instead of `cta`. Both are accepted.

```jsonc
{
  "component": "BrandOutro",
  "props": { "companyName": "Contoso", "cta": "Learn more at contoso.com" }
}
```

---

### LowerThird

Professional name + title overlay bar, typically used with speaker footage.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `name` | string | ✅ | — | Person's name |
| `title` | string | ✅ | — | Job title or role |
| `side` | `"left"` \| `"right"` | | `"left"` | Which side of the screen |
| `accentColor` | string | | — | CSS colour for the accent bar |

```jsonc
{
  "component": "LowerThird",
  "props": { "name": "Jane Doe", "title": "VP of Engineering", "side": "left" }
}
```

---

### AnimatedCaption

Word-highlight or sentence-level caption overlay.

For narration subtitles generated by the SCF compiler, prefer active-line
captions: `panel: false`, `maxWordsPerLine: 5`, `backgroundColor: "transparent"`,
`timingOffsetSec: 0.07`, and a per-line `lineBackgroundColor` plus per-word
`highlightBackgroundColor`.
Only one short transcript line should be visible at a time; boxed caption
panels are opt-in only. Use a positive `timingOffsetSec` when transcript word
timestamps feel slightly ahead of the spoken narration.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `text` | string | ✅ | — | Caption text content |
| `position` | `"top"` \| `"bottom"` | | `"bottom"` | Screen position |
| `fontSize` | number | | — | Font size in pixels |

```jsonc
{
  "component": "AnimatedCaption",
  "props": { "text": "AI-Powered Insights", "position": "bottom", "fontSize": 28 }
}
```

---

## Data Components

### MetricsCard

Single KPI display with optional delta indicator and sparkline.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `label` | string | ✅ | — | Metric name |
| `value` | number \| string | ✅ | — | Current value |
| `prevValue` | number \| string | | — | Previous value (for delta calc) |
| `unit` | string | | — | Unit suffix (e.g. `"%"`, `"ms"`) |
| `deltaText` | string | | — | Explicit delta text override |
| `sparklinePoints` | string | | — | SVG polyline points for sparkline |

```jsonc
{
  "component": "MetricsCard",
  "props": { "label": "Uptime", "value": "99.97", "unit": "%", "prevValue": "99.91" }
}
```

---

### PricingTable

Columnar pricing comparison table. *(Schema-enforced since PR 3.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `tiers` | array of Tier | ✅ | Each tier: `name`, `price`, `features[]`, optional `highlight` |
| `currency` | string | | Currency symbol |
| `billingNote` | string | | Billing-period text |

---

### CompetitiveMatrix

Feature comparison grid. *(Schema-enforced since PR 3.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `products` | string[] | ✅ | Column headers |
| `features` | Feature[] | ✅ | Each: `name`, `values[]` (✓/✗/text) |
| `highlightColumn` | integer | | Zero-based index of the "hero" column |

---

### ROICalculator

Animated ROI projection component. *(Schema-enforced since PR 3.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `currentCost` | number | ✅ | Baseline annual cost |
| `proposedCost` | number | ✅ | Projected cost after change |
| `timeframeMonths` | integer | ✅ | Projection window |
| `currency` | string | | Currency symbol |
| `additionalBenefits` | string[] | | Bullet-point list of qualitative benefits |

---

### BurnDown

Sprint burn-down chart with planned vs actual lines. *(Schema-enforced since PR 4.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `title` | string | ✅ | Chart title |
| `plan` | number[] | ✅ | Planned remaining-work series |
| `actual` | number[] | ✅ | Actual remaining-work series |
| `forecast` | number[] | | Forecast series |
| `todayIndex` | integer | | Current-day index |
| `goalLine` | number | | Target line value |
| `highlights` | array | | Annotation markers |
| `subtitle` | string | | Chart subtitle |

---

### OKRStatus

OKR or goal-tracking card. *(Schema-enforced since PR 4.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `objective` | string | ✅ | Objective text |
| `keyResults` | KR[] | ✅ | Each: `text`, `progress` (0-100), `status` |
| `owner` | string | | Owner name |
| `quarter` | string | | Time period label |

---

### ProgressBar

Labelled animated progress bar. *(Schema-enforced since PR 4.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `label` | string | ✅ | Bar label text |
| `value` | number | ✅ | Current value (0-100) |
| `max` | number | | Maximum value (default 100) |
| `color` | string | | Bar fill colour |
| `showPercent` | boolean | | Show percentage text |

---

## IDE / Demo Components

### VSCodeScene

Full-fidelity VS Code chrome recreation. Accepts **legacy short-form** props
(auto-expanded by `vscode-scene-transformer.mjs`) or **granular chrome** props.

#### Legacy Props (transformer expands these)

| Prop | Type | Description |
|------|------|-------------|
| `stepsHtml` | string | Playback steps HTML injected into the editor |
| `filename` | string | Active file name (tab + explorer) |
| `language` | string | Language identifier for syntax context |
| `statusbarText` | string | Space-separated status-bar items |

#### Chrome Props (full control)

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `showSecondary` | `"true"` \| `"false"` | `"false"` | Show secondary sidebar |
| `showPanel` | `"true"` \| `"false"` | `"false"` | Show bottom panel |
| `showMinimap` | `"true"` \| `"false"` | `"true"` | Show minimap |
| `showBreadcrumb` | `"true"` \| `"false"` | `"true"` | Show breadcrumb bar |
| `activeActivity` | enum | `"explorer"` | `explorer` / `search` / `scm` / `run` / `extensions` / `chat` |
| `titlebarText` | string | | Title bar content |
| `scmBadgeHtml` | string | | SCM badge overlay |
| `primarySidebarTitle` | string | | Left sidebar heading |
| `primarySidebarActionsHtml` | string | | Left sidebar action buttons |
| `primarySidebarBodyHtml` | string | | Left sidebar body |
| `secondarySidebarTitle` | string | | Right sidebar heading |
| `secondarySidebarActionsHtml` | string | | Right sidebar action buttons |
| `secondarySidebarBodyHtml` | string | | Right sidebar body |
| `tabListHtml` | string | | Editor tab strip |
| `breadcrumbHtml` | string | | Breadcrumb bar content |
| `gutterHtml` | string | | Line-number gutter |
| `codeContentHtml` | string | | Main editor content |
| `minimapHtml` | string | | Minimap overlay |
| `panelTabsHtml` | string | | Panel tab strip |
| `panelBodyHtml` | string | | Panel body |
| `statusbarLeftHtml` | string | | Left status-bar items |
| `statusbarRightHtml` | string | | Right status-bar items |

> **Tip:** For simple scenes, use legacy props — the transformer fills all
> chrome slots automatically. Switch to chrome props only when you need
> precise control over specific UI areas.

```jsonc
{
  "component": "VSCodeScene",
  "props": {
    "filename": "app.ts",
    "stepsHtml": "<div data-step='1'>const greeting = 'Hello';</div>",
    "statusbarText": "TypeScript UTF-8 LF"
  }
}
```

---

### AzureDevOpsScene

Reusable synthetic Azure DevOps surface for repository files, code file views,
pull request overviews, diffs, PR Assistant comments, and reviewer activity.
Use anonymized values by default; pass structured arrays for most scenes and
reserve raw `bodyHtml` for bespoke one-off layouts.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | enum | `"repo-files"` | `repo-files`, `file-view`, `pr-overview`, `pr-diff`, `assistant-comment`, or `reviewer-activity` |
| `projectName` | string | `"Commerce Platform"` | Project name shown in the sidebar |
| `repoName` | string | `"Commerce-Financials-Controls"` | Repository chip and breadcrumb text |
| `branchName` | string | `"main"` | Branch/status pill text |
| `files` | array | demo rows | Repo or changed-file rows: `name`, `path`, `meta`, `active` |
| `tree` | array | demo tree | Folder tree rows: `name`, `type`, `indent`, `active` |
| `fileLocation` | string | demo location | Display-only folder or repo location for the active file |
| `codeLines` | array | demo code | Strings or `{ number, text, highlight }` rows |
| `diffLines` | array | demo diff | `{ type, oldNumber, newNumber, text }` rows |
| `checks` | array | demo checks | PR check rows: `title`, `subtitle`, `status` |
| `reviewers` | array | demo reviewers | Reviewer rows: `name`, `role`, `status`, `initials` |
| `impactRows` | array | demo impact rows | PR Assistant table rows for impacted contracts and owners |
| `recommendations` | string[] | demo recommendations | Compact PR Assistant recommendation bullets |
| `activity` | array | demo timeline | Reviewer activity rows: `title`, `subtitle`, `status`, `time` |
| `bodyHtml` | string | generated | Raw body override for precise custom layouts |

```jsonc
{
  "component": "AzureDevOpsScene",
  "props": {
    "variant": "assistant-comment",
    "projectName": "Commerce Platform",
    "repoName": "Commerce-Financials-Controls",
    "impactRows": [
      {
        "contract": "BillingGroup",
        "changedFile": "schemas/billing_group.py",
        "affectedRepo": "Collector Service",
        "owner": "Priya Shah",
        "reason": "Shared field set changed"
      }
    ]
  }
}
```

---

### TerminalScene

Terminal emulator with single-pane or split-pane layouts.

| Prop | Type | Description |
|------|------|-------------|
| `theme` | string | Colour theme name |
| `titlebar` | string | Title bar text |
| `linesHtml` | string | Raw HTML for terminal output lines |
| `tabsHtml` | string | Tab strip HTML |
| `leftPaneTitle` | string | Split-pane: left title |
| `leftPaneContentHtml` | string | Split-pane: left content |
| `rightPaneTitle` | string | Split-pane: right title |
| `rightPaneContentHtml` | string | Split-pane: right content |
| `statusHtml` | string | Status bar HTML |

> **Note:** Use either `linesHtml` (single pane) or the `leftPane*`/`rightPane*`
> props (split pane) — not both.

```jsonc
{
  "component": "TerminalScene",
  "props": {
    "titlebar": "bash — 120×40",
    "linesHtml": "<div class='line'>$ npm test</div><div class='line ok'>All 47 tests passed</div>"
  }
}
```

---

### TerminalCast

Animated terminal recording playback with shell-specific theming.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `shellTheme` | enum | | `"powershell"` | `powershell` / `bash` / `cloudshell` / `python` |
| `linesHtml` | string | ✅ | — | Animated lines with `data-delay` attributes |
| `titleHtml` | string | | — | Title bar content |
| `windowChrome` | `"macos"` \| `"windows"` | | — | Window chrome style |
| `cursorStyle` | `"block"` \| `"line"` \| `"underline"` | | `"block"` | Cursor shape |

> **Shell themes:** `powershell` (blue), `bash` (dark), `cloudshell` (teal), `python` (gold accent).

---

### ScreenDemoFrame

Device frame wrapping external content (browser, macOS window, phone, tablet).

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `src` | string | ✅ | — | URL or path to framed content |
| `frameStyle` | enum | | `"browser"` | `browser` / `macos-window` / `phone-portrait` / `phone-landscape` / `tablet` |
| `urlBar` | string | | — | URL shown in browser chrome |
| `windowTitle` | string | | — | Title for macOS-window chrome |
| `theme` | `"light"` \| `"dark"` | | `"dark"` | Frame colour theme |
| `shadow` | string | | — | Drop shadow (`"true"` / `"false"`) |
| `cornerRadius` | number | | — | Border radius in pixels |
| `scale` | number | | — | Scale factor (0 < scale ≤ 2) |
| `cursorPath` | string | | — | JSON-encoded cursor animation path |

```jsonc
{
  "component": "ScreenDemoFrame",
  "props": {
    "src": "https://app.contoso.com/dashboard",
    "frameStyle": "browser",
    "urlBar": "app.contoso.com/dashboard"
  }
}
```

---

## Engagement Components

### CTABlock

Call-to-action card with eyebrow, title, body, and action button.

| Prop | Type | Required | Default | Description |
|------|------|:--------:|---------|-------------|
| `title` | string | ✅ | — | Headline |
| `ctaText` | string | ✅ | — | Button / action label |
| `eyebrow` | string | | — | Small text above the title |
| `body` | string | | — | Body copy |
| `contact` | string | | — | Contact info |
| `accentColor` | string | | — | CSS colour for button / accent |

```jsonc
{
  "component": "CTABlock",
  "props": { "title": "Ready to start?", "ctaText": "Book a Demo", "body": "See Slate in action." }
}
```

---

### CustomerStory

Customer testimonial / case-study card. *(Schema-enforced since PR 3.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `customerName` | string | ✅ | Customer organisation name |
| `quote` | string | ✅ | Testimonial quote |
| `logoSrc` | string | | Customer logo |
| `attribution` | string | | Person + title attribution |
| `metrics` | Metric[] | | Key results: `label`, `value`, `improvement` |

---

### Quiz

Interactive quiz / poll component. *(Schema-enforced since PR 5.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `question` | string | ✅ | Question text |
| `options` | Option[] | ✅ | Each: `text`, `isCorrect` |
| `explanation` | string | | Post-answer explanation |
| `theme` | string | | Visual theme |

---

### AskTheAudience

Live audience polling component. *(Schema-enforced since PR 6.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `prompt` | string | ✅ | Poll question |
| `options` | string[] | ✅ | Answer choices |
| `resultsJson` | string | | Pre-seeded results JSON |
| `responseCount` | integer | | Response counter |
| `pollState` | `"live"` \| `"closed"` | | Poll lifecycle state |
| `source` | enum | | `Forms` / `Mentimeter` / `synthetic` |
| `ctaNote` | string | | Instruction text |
| `viz` | `"bars"` \| `"donut"` | | Visualisation style |

---

## Planning Components

### Roadmap

Timeline / roadmap visualisation. *(Schema-enforced since PR 3.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `title` | string | ✅ | Roadmap title |
| `phases` | Phase[] | ✅ | Each: `name`, `items[]`, `status` |
| `timeScale` | string | | Human-readable time range |
| `highlights` | string[] | | Key milestones to emphasise |

---

### ReleaseNotes

Release / changelog card. *(Schema-enforced since PR 5.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `version` | string | ✅ | Version string |
| `date` | string | ✅ | Release date |
| `sections` | Section[] | ✅ | Each: `title`, `items[]` (markdown-like) |
| `highlights` | string[] | | TL;DR items |

---

## Education Components

### TerminologyCard

Glossary / definition card. *(Schema-enforced since PR 5.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `term` | string | ✅ | Term being defined |
| `definition` | string | ✅ | Plain-language definition |
| `category` | string | | Grouping label |
| `relatedTerms` | string[] | | Cross-references |
| `example` | string | | Usage example |

---

## Overlay Components

### PresenterBug

Small always-on-top presenter PIP / bug. *(Schema-enforced since PR 6.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `src` | string | ✅ | Image / video source |
| `position` | enum | | `bottom-right` / `bottom-left` / `top-right` / `top-left` |
| `size` | enum | | `small` / `medium` / `large` |
| `shape` | enum | | `circle` / `rounded-square` |
| `borderColor` | string | | CSS colour for ring |

---

### EventBranding

Persistent event-branding overlay (banner + logo). *(Schema-enforced since PR 6.)*

| Prop | Type | Required | Description |
|------|------|:--------:|-------------|
| `eventName` | string | ✅ | Event name text |
| `logoSrc` | string | | Event logo |
| `tagline` | string | | Sub-heading |
| `position` | enum | | `top` / `bottom` |
| `style` | enum | | `banner` / `minimal` / `full` |
| `primaryColor` | string | | CSS colour |

---

## Scene-Level Properties

These properties live directly on the **scene object** (not inside `props`):

| Property | Type | Description |
|----------|------|-------------|
| `id` | string | Unique scene identifier |
| `duration` | number | Scene length in seconds |
| `component` | string | Component name from the enum |
| `props` | object | Component-specific properties (see above) |
| `layers` | array | Layer-based scene definition (alternative to `component`) |
| `narration` | string | Path to narration audio file |
| `narrationStartSec` | number | Offset (seconds) before narration begins |
| `transition` | string | Transition to next scene |
| `background` | string | Scene background colour / image |
| `visual_prompt` | string | Prompt for AI image generation |

> **Note:** Structured visuals and AI videos are produced as files at the Assets stage by the `structured_image` and `foundry_video_gen` tools, then referenced via `layers` (image/video) or components like `ScreenDemoFrame` and `SlideRenderer`.
| `title` | string | Scene title (for internal labelling) |
| `model_hint` | string | Override image model routing (`faces` / `photo` / `creative`) |

---

## Unschematised Components

The following components are in the SCF `component` enum but do **not** yet
have `$defs` prop schemas. They accept any props without validation.

Accordion, AccordionStack, AgendaTimeline, AnnotatedScreenshot, BeforeAfter,
BulletList, Callout, CenteredQuote, Checklist, CodeBlock, CodeWalkthrough,
ComparisonSlider, DataTable, FeatureGrid, FlipCard, FlowDiagram, GanttChart,
HeatMap, HeroImage, IconGrid, ImageGallery, KanbanBoard, LiveCode, LogoCloud,
MapView, MindMap, NetworkGraph, NumberCounter, ParallaxScroll, PieChart,
ProcessFlow, QuoteCarousel, ScatterPlot, SplitScreen, StackedBar, StatCounter,
StepByStep, StickyNote, SwipeCarousel, TestimonialWall, Timeline, TreeMap,
VideoEmbed, WordCloud.

Product-showcase composites: BookPageMetrics, ComponentOverlay, MetricStack.
Synthetic data surfaces: KustoExplorerScene.
Premium motion / WebGL: ThreeScene, DeviceStage3D, HTMLTextureWall.

### KustoExplorerScene

High-fidelity Kusto Explorer desktop-style query surface for KQL investigations.
Use it when a scene needs a credible connection tree, query editor, result grid,
and timed focus highlights without recording a real Kusto session.

Important: pass mock data only. Do not pass real tenant, cluster, database,
table, query, file path, or user/account names.

| Prop | Type | Default | Notes |
|------|------|---------|-------|
| `connections` | array | mock finance-control tree | Rows with `name`, `type`, `depth`, `expanded`, `selected`. |
| `tabs` | array | three mock tabs | Query tab labels with optional `active`. |
| `address` | string | mock cluster/database locator | Shown above the editor. |
| `queryLines` | string[] | mock classifier KQL | Lines rendered in the editor with lightweight KQL coloring. |
| `highlightLines` | integer[] | `[1,3,4,8,9]` | 1-based editor lines highlighted over time. |
| `results` | object | mock classifier buckets | `{ columns, rows }` result grid. |
| `requestId` | string | mock request id | Status bar value. |
| `visualBeats` | array | — | Motion-beat metadata for validation and review. |
| `callout` | object | mock workspace callout | `{ eyebrow, title, body }` floating glass card. |

### Premium motion / WebGL components

These three components share the deterministic three.js authoring contract
described in `skills/core/render/three-js.md` (no `requestAnimationFrame`,
register through `window.__slateThree`, render at `compositionTime - SCENE_START`,
seeded RNG, `setPixelRatio(1)`).

**ThreeScene** — generic deterministic 3D hero stage with seeded geometry,
lighting and a DOM copy overlay. Use for premium moments that don't fit a
more specific 3D component.

**DeviceStage3D** — renders a screenshot or texture on a tilted screen
plane wrapped in a browser chrome / device bezel / glass panel surround.

| Prop | Type | Default | Notes |
|------|------|---------|-------|
| `title` | string | `''` | Large foreground copy (DOM overlay, not in 3D). |
| `subtitle` | string | `''` | Supporting line under the title. |
| `screenSrc` | string | `''` | Path to the screenshot / texture image. Auto-staged into the localhost server root before render. If absent or load fails, a deterministic placeholder texture is used. |
| `mode` | `browser` \| `device` \| `glass` | `browser` | Surround treatment: traffic-light browser bar; tablet/laptop bezel; semi-transparent glass panel. |
| `primaryColor` | string (hex) | `#8B5CF6` | Rim light + glow tint. |
| `accentColor` | string (hex) | `#E7D7A2` | Secondary point light. |
| `seed` | string | scene id + title | Seeds the RNG used for any per-render jitter. |

**HTMLTextureWall** — wall or carousel of card planes. Each plane is
textured with either a CanvasTexture rendered from a `cards` entry (exact
text — never AI-generated) or a pre-rendered image from `textureSrcs`.

| Prop | Type | Default | Notes |
|------|------|---------|-------|
| `title` | string | `''` | Top-left header copy (DOM overlay). |
| `cards` | array of `{title, subtitle?, kicker?}` | `[]` | When non-empty, used as the source of truth — each card becomes a CanvasTexture. Wins over `textureSrcs`. |
| `textureSrcs` | array of strings | `[]` | Image paths used when `cards` is empty. Each path is auto-staged into the localhost server root. |
| `mode` | `wall` \| `carousel` | `wall` | `wall` lays cards out in a 2–4-column grid with subtle depth jitter; `carousel` arranges them around a vertical axis with steady rotation. |
| `primaryColor` | string (hex) | `#8B5CF6` | Card top-stripe accent. |
| `accentColor` | string (hex) | `#E7D7A2` | Card kicker color + corner glow. |
| `seed` | string | scene id + mode | Seeds depth jitter on the wall layout. |

> **Contributing:** To add a prop schema for any of these, add a `$defs` entry
> and corresponding `allOf` if/then block in `schemas/scf-v1.0.schema.json`.

---

*Generated from SCF v1.0 schema — last updated 2026-07.*
